echo "🔨 Running pre-commit checks..."

# Only check build, don't run full tests to speed up development
echo "📦 Testing build..."
npm run build || {
    echo "❌ Build failed! Fix the build errors before committing."
    echo "💡 Tip: Run 'npm run build' to see detailed errors"
    exit 1
}

echo "✅ Pre-commit checks passed!"
